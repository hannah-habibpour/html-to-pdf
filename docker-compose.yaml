# Docker Compose file for running the Puppeteer service
services:
  puppeteer-service:
    build: .
    ports:
      - "8000:8000"
    environment:
      - PUPPETEER_EXECUTABLE_PATH=/usr/bin/chromium-browser
      - PUPPETEER_SKIP_CHROMIUM_DOWNLOAD=true
